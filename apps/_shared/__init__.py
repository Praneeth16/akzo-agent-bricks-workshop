"""Shared, self-contained package reused by the AkzoNobel apps + notebooks.

Holds the PROVEN write/read modules (`databricks_client`, `lakebase`) and the
`action_plane` package — the single governed plane every agent acts through.
"""
